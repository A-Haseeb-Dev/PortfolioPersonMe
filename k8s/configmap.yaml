apiVersion: v1
kind: ConfigMap
metadata:
  name: portfolio-config
  namespace: portfolio
  labels:
    app: portfolio
data:
  NODE_ENV: "production"
  NEXT_PUBLIC_APP_URL: "https://anas-portfolio.vercel.app"
  NEXT_TELEMETRY_DISABLED: "1"
